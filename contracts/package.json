{
  "name": "clipr-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "CLIPR escrow — campaign budgets held onchain, paid out per verified view against a signed settlement voucher.",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "export-abi": "hardhat run scripts/export-abi.ts",
    "deploy:local": "hardhat run scripts/deploy.ts --network hardhat",
    "deploy:robinhood": "hardhat run scripts/deploy.ts --network robinhood",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.2.0",
    "@types/mocha": "^10.0.0",
    "@types/node": "^20.0.0",
    "chai": "^4.2.0",
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.1.0"
  }
}
